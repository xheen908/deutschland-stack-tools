# Contributing

We welcome contributions to `deutschland-stack-tools`! 

## Getting Started
1. Fork the repository
2. Run `npm install`
3. Run `npm run test` to make sure tests pass
4. Use `tools/download-tools.sh` to fetch validation engines
5. Submit a Pull Request

Please ensure your code follows the existing TypeScript conventions and passes the ESLint checks.
